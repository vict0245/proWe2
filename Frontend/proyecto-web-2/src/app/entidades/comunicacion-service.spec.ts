import { ComunicacionService } from './comunicacion-service';

describe('ComunicacionService', () => {
  it('should create an instance', () => {
    expect(new ComunicacionService()).toBeTruthy();
  });
});
